name: Independent Daily Screener V2 Full

on:
  push:
    branches:
      - experiment/independent-daily-screener-v2
    paths:
      - .github/workflows/independent-daily-lab-v2-full.yml

concurrency:
  group: independent-daily-lab-v2-full
  cancel-in-progress: true

jobs:
  full:
    runs-on: ubuntu-latest
    timeout-minutes: 150
    permissions:
      contents: read

    steps:
      - name: Checkout V2 branch
        uses: actions/checkout@v7
        with:
          ref: experiment/independent-daily-screener-v2

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Compile
        run: python -m py_compile independent_daily_lab_v2.py screen_big_money.py screen_entry.py

      - name: Run V2 full five-year / one-year holdout
        run: |
          python independent_daily_lab_v2.py \
            --max-workers 48 \
            --history-days 1825 \
            --holdout-days 365 \
            --output-dir reports_independent_lab_v2

      - name: Upload V2 full report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: independent-daily-lab-v2-full
          path: reports_independent_lab_v2/
          if-no-files-found: ignore
          retention-days: 30
